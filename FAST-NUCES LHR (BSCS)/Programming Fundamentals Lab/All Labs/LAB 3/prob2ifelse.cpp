#include <iostream>
using namespace std;

int main (){
	int y;
	cout <<"Please enter an number: ";
	cin >> y;

	if (y==1)
		cout<<"Monday";
		if (y==2)
cout<<"Tuesday";
		if (y==3)
			cout<<"Wednesday";
			if (y==4)
				cout<<"Thursday";
				if (y==5)
					cout<<"Friday";
					if (y==6)
						cout<<"Saturday";
						if (y==7)
							cout<<"Sunday";
							
	system("pause");
	return 0;
}